import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    common: {
      grey: "#C7C7C7",
    },
    primary: {
      main: "#F0932B",
      light: "#f3ab5b",
    },
    secondary: {
      main: "#2A1803",
    },
    background: {
      primary: "#FDF4E9",
    },
  },
});

export default theme;
