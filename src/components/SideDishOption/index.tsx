import React from "react";
import { Typography } from "@mui/material";
import { SideDistType } from "shared/types";

const SideDishOption = ({ name, price }: SideDistType) => {
  return <div></div>;
};

export default SideDishOption;
