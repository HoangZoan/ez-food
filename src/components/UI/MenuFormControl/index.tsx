import { FormControl } from "components/UI/FormComponents";
import { styled } from "shared/theme";

const MenuFormControl = styled(FormControl)({
  gridTemplateColumns: "1fr 3fr",
  columnGap: "3.6rem",
});

export default MenuFormControl;
